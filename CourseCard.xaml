<UserControl x:Class="EduPro.CourseCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EduPro"
             xmlns:converter="clr-namespace:EduPro.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="160" d:DesignWidth="500">
    <UserControl.Resources>
        <converter:IntToVisibilityConverter x:Key="IntToVisibilityConverter"/>
    </UserControl.Resources>
    <Border BorderBrush="Black" Background="{Binding BackGroundColor}" Margin="10 5 5 5" BorderThickness="3">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <Image Source="{Binding ImageFullPath}" Height="110"/>
            </StackPanel>
            <StackPanel Grid.Column="1">
                <Label Content="Название"/>
                <Label Content="{Binding Name}" FontWeight="{Binding TitleFontWeight}"/>
                <Label Content="Направление"/>
                <Label Content="{Binding CourseDirection.Name}"/>
                <Label Content="Длительность"/>
                <Label Content="{Binding Duration}"/>
                <Label Content="Дата начала"/>
                <Label Content="{Binding Date}"/>
            </StackPanel>
            
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <Label Content="Стоимость"/>
                    <Label Content="{Binding Price}"/>
                </StackPanel>

                <StackPanel Grid.Row="2" VerticalAlignment="Bottom">
                    <Label Content="Свободных мест"/>
                    <Label Content="{Binding FreeSeat}"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
