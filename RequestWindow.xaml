<Window x:Class="EduPro.RequestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EduPro"
        mc:Ignorable="d"
        Title="Заявка" 
        Height="450" 
        Width="500"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Grid.Row="0" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Клиент:*" Width="120" VerticalAlignment="Center"/>
            <ComboBox x:Name="UserComboBox" Width="250" DisplayMemberPath="FullName" FontSize="14"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Курс:*" Width="120" VerticalAlignment="Center"/>
            <ComboBox x:Name="CourseComboBox" Width="250"  DisplayMemberPath="Name" FontSize="14" SelectionChanged="CourseComboBox_SelectionChanged"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock x:Name="FreeSeatsText" Foreground="#666666" FontStyle="Italic"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Дата:*" Width="120" VerticalAlignment="Center"/>
            <DatePicker x:Name="DatePicker" Width="250" FontSize="14"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Статус:*" Width="120" VerticalAlignment="Center"/>
            <ComboBox x:Name="StatusComboBox" Width="250" DisplayMemberPath="Name" FontSize="14"/>
        </StackPanel>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="DeleteButton" Content="Удалить" Click="DeleteButton_Click" Width="80" Height="30" Margin="0,0,10,0" Background="#F44336" Foreground="White" Visibility="Collapsed"/>
            <Button x:Name="SaveButton" Content="Сохранить" Click="SaveButton_Click" Width="80" Height="30" Margin="0,0,10,0" Background="#4CAF50" Foreground="White"/>
            <Button x:Name="CancelButton" Content="Отмена" Click="CancelButton_Click" Width="80" Height="30"/>
        </StackPanel>
    </Grid>
</Window>